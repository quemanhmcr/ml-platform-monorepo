# =============================================================================
# ConfigMap for ML Recommendation Inference
# =============================================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: ml-inference-config
data:
  config.yaml: |
    component:
      name: inference
      version: "1.0.0"
    
    inference:
      api_host: "0.0.0.0"
      api_port: 8080
    
    logging:
      level: "INFO"

