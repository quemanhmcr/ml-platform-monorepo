# =============================================================================
# Workflow: Build Inference Image and Update GitOps
#
# Purpose
# - Build and push inference Docker image to ECR
# - Automatically update image tag in GitOps repo for ArgoCD sync
# - Triggered on push/PR to components/inference/ or main/develop branches
#
# Flow
# 1. Build inference image with immutable tags
# 2. Push to ECR
# 3. Update GitOps repo with new image tag
# 4. ArgoCD automatically syncs deployment to cluster
# =============================================================================
name: Build Inference and Update GitOps

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'ml-source-code/components/inference/**'
      - '.github/workflows/source-code-build-inference.yml'
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'ml-source-code/components/inference/**'

permissions:
  id-token: write  # Required for OIDC authentication with AWS
  contents: read   # Required for checkout

env:
  PYTHON_VERSION: "3.10"
  COMPONENT: "inference"
  REPO_PREFIX: "ml-fashion-recommender"

jobs:
  build-push-and-update-gitops:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ml-source-code
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Derive environment and tag strategy
        id: env
        shell: bash
        run: |
          set -euo pipefail
          
          # Determine environment and tag prefix based on branch
          if [[ "${GITHUB_REF##*/}" == "main" ]]; then
            ENV="production"
            TAG_PREFIX="main"
            GITOPS_OVERLAY="production"
          elif [[ "${GITHUB_REF##*/}" == "develop" ]]; then
            ENV="staging"
            TAG_PREFIX="develop"
            GITOPS_OVERLAY="staging"
          else
            ENV="staging"
            TAG_PREFIX="develop"
            GITOPS_OVERLAY="staging"
          fi
          
          # Extract SHA and timestamp for tag generation
          FULL_SHA="${GITHUB_SHA}"
          SHORT_SHA="${GITHUB_SHA::7}"
          BUILD_TIMESTAMP=$(date -u +"%Y%m%d-%H%M%S")
          RUN_ID="${GITHUB_RUN_ID}"
          
          # BigTech ML Platform Standard: Immutable tag strategy
          # Primary tag: full SHA (always unique, immutable)
          # Latest tag: branch-latest (mutable, points to latest build)
          # Build tag: branch-{short-sha}-{timestamp} (for traceability)
          
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            # PR builds: use PR number + full SHA
            PRIMARY_TAG="pr-${{ github.event.pull_request.number }}-${FULL_SHA}"
            BUILD_TAG="pr-${{ github.event.pull_request.number }}-${SHORT_SHA}-${BUILD_TIMESTAMP}"
            LATEST_TAG="${TAG_PREFIX}-latest"
          else
            # Branch builds: use branch + full SHA for primary (immutable)
            PRIMARY_TAG="${TAG_PREFIX}-${FULL_SHA}"
            BUILD_TAG="${TAG_PREFIX}-${SHORT_SHA}-${BUILD_TIMESTAMP}"
            LATEST_TAG="${TAG_PREFIX}-latest"
          fi
          
          echo "env=${ENV}" >> $GITHUB_OUTPUT
          echo "tag_prefix=${TAG_PREFIX}" >> $GITHUB_OUTPUT
          echo "sha7=${SHORT_SHA}" >> $GITHUB_OUTPUT
          echo "full_sha=${FULL_SHA}" >> $GITHUB_OUTPUT
          echo "build_timestamp=${BUILD_TIMESTAMP}" >> $GITHUB_OUTPUT
          echo "run_id=${RUN_ID}" >> $GITHUB_OUTPUT
          echo "primary_tag=${PRIMARY_TAG}" >> $GITHUB_OUTPUT
          echo "build_tag=${BUILD_TAG}" >> $GITHUB_OUTPUT
          echo "latest_tag=${LATEST_TAG}" >> $GITHUB_OUTPUT
          echo "gitops_overlay=${GITOPS_OVERLAY}" >> $GITHUB_OUTPUT
          
          echo "📋 Tag Strategy (BigTech ML Platform Standard):"
          echo "   Environment: ${ENV}"
          echo "   Primary Tag (immutable): ${PRIMARY_TAG}"
          echo "   Build Tag (traceable): ${BUILD_TAG}"
          echo "   Latest Tag (mutable): ${LATEST_TAG}"

      - name: Load AWS configuration (Secrets-only, Zero-Trust)
        id: aws-config-loader
        env:
          AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          AWS_IAM_ROLE_ARN: ${{ secrets.AWS_IAM_ROLE_ARN }}
          AWS_ECR_REGISTRY: ${{ secrets.AWS_ECR_REGISTRY }}
        run: |
          set -euo pipefail
          
          # Validate all required secrets are present
          MISSING_SECRETS=()
          
          if [ -z "${AWS_ACCOUNT_ID:-}" ] || [ "${AWS_ACCOUNT_ID}" = "null" ]; then
            MISSING_SECRETS+=("AWS_ACCOUNT_ID")
          fi
          
          if [ -z "${AWS_REGION:-}" ] || [ "${AWS_REGION}" = "null" ]; then
            MISSING_SECRETS+=("AWS_REGION")
          fi
          
          if [ -z "${AWS_IAM_ROLE_ARN:-}" ] || [ "${AWS_IAM_ROLE_ARN}" = "null" ]; then
            MISSING_SECRETS+=("AWS_IAM_ROLE_ARN")
          fi
          
          if [ -z "${AWS_ECR_REGISTRY:-}" ] || [ "${AWS_ECR_REGISTRY}" = "null" ]; then
            MISSING_SECRETS+=("AWS_ECR_REGISTRY")
          fi
          
          if [ ${#MISSING_SECRETS[@]} -gt 0 ]; then
            echo "❌ Error: Missing required GitHub Secrets:" >&2
            printf "   - %s\n" "${MISSING_SECRETS[@]}" >&2
            exit 1
          fi
          
          # Validate formats
          if [[ ! "$AWS_ACCOUNT_ID" =~ ^[0-9]{12}$ ]]; then
            echo "❌ Error: Invalid account_id format: $AWS_ACCOUNT_ID" >&2
            exit 1
          fi
          
          if [[ ! "$AWS_REGION" =~ ^[a-z]{2}-[a-z]+-[0-9]+$ ]]; then
            echo "❌ Error: Invalid region format: $AWS_REGION" >&2
            exit 1
          fi
          
          if [[ ! "$AWS_IAM_ROLE_ARN" =~ ^arn:aws:iam::[0-9]{12}:role/ ]]; then
            echo "❌ Error: Invalid IAM role ARN format: $AWS_IAM_ROLE_ARN" >&2
            exit 1
          fi
          
          if [[ ! "$AWS_ECR_REGISTRY" =~ ^[0-9]{12}\.dkr\.ecr\.[a-z0-9-]+\.amazonaws\.com$ ]]; then
            echo "❌ Error: Invalid ECR registry format: $AWS_ECR_REGISTRY" >&2
            exit 1
          fi
          
          # Mask sensitive values
          echo "::add-mask::$AWS_ACCOUNT_ID"
          echo "::add-mask::$AWS_IAM_ROLE_ARN"
          echo "::add-mask::$AWS_ECR_REGISTRY"
          
          echo "✅ AWS configuration loaded from GitHub Secrets"
          
          # Output values
          echo "account_id=${AWS_ACCOUNT_ID}" >> $GITHUB_OUTPUT
          echo "region=${AWS_REGION}" >> $GITHUB_OUTPUT
          echo "iam_role_arn=${AWS_IAM_ROLE_ARN}" >> $GITHUB_OUTPUT
          echo "ecr_registry=${AWS_ECR_REGISTRY}" >> $GITHUB_OUTPUT

      - name: Configure AWS credentials (OIDC AssumeRole)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ steps.aws-config-loader.outputs.iam_role_arn }}
          aws-region: ${{ steps.aws-config-loader.outputs.region }}
          role-session-name: GitHubActions-${{ github.run_id }}-${{ github.run_attempt }}
          retry-max-attempts: 0
          disable-retry: true

      - name: Verify caller identity
        run: aws sts get-caller-identity

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push inference image
        id: docker
        env:
          ECR_REGISTRY: ${{ steps.aws-config-loader.outputs.ecr_registry }}
          REGION: ${{ steps.aws-config-loader.outputs.region }}
          COMPONENT: ${{ env.COMPONENT }}
          REPO_PREFIX: ${{ env.REPO_PREFIX }}
          PRIMARY_TAG: ${{ steps.env.outputs.primary_tag }}
          BUILD_TAG: ${{ steps.env.outputs.build_tag }}
          LATEST_TAG: ${{ steps.env.outputs.latest_tag }}
          FULL_SHA: ${{ steps.env.outputs.full_sha }}
          BUILD_TIMESTAMP: ${{ steps.env.outputs.build_timestamp }}
          RUN_ID: ${{ steps.env.outputs.run_id }}
        run: |
          set -euo pipefail
          
          REPO_URI="${ECR_REGISTRY}/${REPO_PREFIX}/${COMPONENT}"
          
          echo "🔨 Building inference image..."
          echo "   Repository: ${REPO_URI}"
          echo "   Primary Tag (immutable): ${PRIMARY_TAG}"
          echo "   Build Tag (traceable): ${BUILD_TAG}"
          echo "   Latest Tag (mutable): ${LATEST_TAG}"
          echo "   Build Timestamp: ${BUILD_TIMESTAMP}"
          echo "   Run ID: ${RUN_ID}"
          
          # Validate component directory exists
          if [ ! -d "components/${COMPONENT}" ]; then
            echo "❌ Error: Component directory not found: components/${COMPONENT}"
            exit 1
          fi
          
          # Validate Dockerfile exists
          if [ ! -f "components/${COMPONENT}/Dockerfile" ]; then
            echo "❌ Error: Dockerfile not found: components/${COMPONENT}/Dockerfile"
            exit 1
          fi
          
          # Build Docker image with immutable tags (BigTech ML Platform Standard)
          # Primary tag: full SHA (immutable, always unique)
          # Build tag: short SHA + timestamp (for traceability)
          # Latest tag: branch-latest (mutable, for convenience)
          docker build \
            -t "${REPO_URI}:${PRIMARY_TAG}" \
            -t "${REPO_URI}:${BUILD_TAG}" \
            -t "${REPO_URI}:${LATEST_TAG}" \
            -f "components/${COMPONENT}/Dockerfile" \
            "components/${COMPONENT}/"
          
          echo "✅ Build completed"
          echo "📋 Tags created:"
          echo "   - ${PRIMARY_TAG} (immutable primary)"
          echo "   - ${BUILD_TAG} (traceable build)"
          echo "   - ${LATEST_TAG} (mutable latest)"
          
          # Push images with error handling for immutable tags
          echo "🚀 Pushing images to ECR..."
          
          # Function to push with error handling for immutable tags
          # Returns 0 on success (including when tag already exists)
          push_image() {
            local tag=$1
            local is_required=${2:-true}
            local push_output
            
            push_output=$(docker push "${REPO_URI}:${tag}" 2>&1)
            local push_exit=$?
            
            if [ $push_exit -eq 0 ]; then
              echo "✅ Successfully pushed ${REPO_URI}:${tag}"
              return 0
            else
              # Check if error is due to immutable tag already existing
              if echo "$push_output" | grep -qi "already exists.*immutable\|cannot be overwritten.*immutable"; then
                echo "⚠️  Tag ${tag} already exists in ECR (immutable policy), skipping..."
                echo "   This is expected if content is identical - tag is already correct"
                return 0  # Not an error - tag exists with same content
              else
                if [ "$is_required" = "true" ]; then
                  echo "❌ Failed to push required tag: ${tag}"
                  echo "Error output: $push_output"
                  return 1
                else
                  echo "⚠️  Failed to push optional tag: ${tag} (non-fatal)"
                  echo "Error output: $push_output"
                  return 0
                fi
              fi
            fi
          }
          
          # Push strategy (BigTech ML Platform Standard):
          # 1. Primary tag (full SHA) - REQUIRED, always unique
          # 2. Build tag (short SHA + timestamp) - OPTIONAL, for traceability
          # 3. Latest tag (branch-latest) - OPTIONAL, mutable reference
          
          # Push primary tag (immutable, full SHA - always unique per commit)
          echo "📤 Pushing primary tag (immutable): ${PRIMARY_TAG}"
          if ! push_image "${PRIMARY_TAG}" true; then
            echo "❌ Failed to push primary tag - this is required"
            exit 1
          fi
          
          # Push build tag (for traceability)
          echo "📤 Pushing build tag (traceable): ${BUILD_TAG}"
          push_image "${BUILD_TAG}" false || true
          
          # Push latest tag (mutable, may fail if immutable policy enabled)
          # Latest tag is for convenience only, not used in GitOps
          echo "📤 Pushing latest tag (mutable): ${LATEST_TAG}"
          push_image "${LATEST_TAG}" false || true
          
          # BigTech ML Platform Standard: Always use PRIMARY_TAG (immutable) for GitOps
          # This ensures reproducibility and immutability in production
          GITOPS_TAG="${PRIMARY_TAG}"
          
          echo "✅ Image push completed"
          echo "📋 GitOps will use tag: ${GITOPS_TAG} (immutable primary tag)"
          echo "📋 Primary tag (immutable): ${PRIMARY_TAG}"
          echo "📋 Build tag (traceable): ${BUILD_TAG}"
          echo "📋 Latest tag (mutable, convenience only): ${LATEST_TAG}"
          
          # Output for GitOps update
          echo "repo_uri=${REPO_URI}" >> $GITHUB_OUTPUT
          echo "image_tag=${PRIMARY_TAG}" >> $GITHUB_OUTPUT
          echo "build_tag=${BUILD_TAG}" >> $GITHUB_OUTPUT
          echo "latest_tag=${LATEST_TAG}" >> $GITHUB_OUTPUT
          echo "gitops_tag=${GITOPS_TAG}" >> $GITHUB_OUTPUT

      - name: Check GitOps secrets
        id: gitops-check
        env:
          GITOPS_TOKEN: ${{ secrets.GITOPS_TOKEN }}
          GITOPS_REPO: ${{ secrets.GITOPS_REPO }}
        run: |
          set -euo pipefail
          
          if [[ -z "${GITOPS_TOKEN:-}" ]] || [[ "${GITOPS_TOKEN}" == "null" ]]; then
            echo "⚠️  Warning: GITOPS_TOKEN not set, skipping GitOps update"
            echo "enabled=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          if [[ -z "${GITOPS_REPO:-}" ]] || [[ "${GITOPS_REPO}" == "null" ]]; then
            echo "⚠️  Warning: GITOPS_REPO not set, skipping GitOps update"
            echo "enabled=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "✅ GitOps secrets found"
          echo "enabled=true" >> $GITHUB_OUTPUT

      - name: Update GitOps repo image tag
        env:
          GITOPS_TOKEN: ${{ secrets.GITOPS_TOKEN }}
          GITOPS_REPO: ${{ secrets.GITOPS_REPO }}
        if: steps.gitops-check.outputs.enabled == 'true' && github.event_name != 'pull_request'
        run: |
          set -euo pipefail
          
          ENV="${{ steps.env.outputs.env }}"
          OVERLAY="${{ steps.env.outputs.gitops_overlay }}"
          REPO_URI="${{ steps.docker.outputs.repo_uri }}"
          # BigTech ML Platform Standard: Always use primary tag (immutable, full SHA) for GitOps
          # This ensures reproducibility and immutability in production
          IMAGE_TAG="${{ steps.docker.outputs.gitops_tag || steps.docker.outputs.image_tag }}"
          
          echo "📝 Updating GitOps repo..."
          echo "   Environment: ${ENV}"
          echo "   Overlay: ${OVERLAY}"
          echo "   Image: ${REPO_URI}:${IMAGE_TAG}"
          
          # Clone GitOps repo
          WORKDIR=$(mktemp -d)
          git config --global user.name "gh-actions-bot"
          git config --global user.email "gh-actions-bot@users.noreply.github.com"
          
          # Extract repo URL and add token
          if [[ "${GITOPS_REPO}" == https://* ]]; then
            REPO_URL="${GITOPS_REPO/https:\/\//https://${GITOPS_TOKEN}@}"
          elif [[ "${GITOPS_REPO}" == http://* ]]; then
            REPO_URL="${GITOPS_REPO/http:\/\//http://${GITOPS_TOKEN}@}"
          else
            # Assume format: github.com/owner/repo or owner/repo
            REPO_URL="https://${GITOPS_TOKEN}@${GITOPS_REPO}"
          fi
          
          echo "🔐 Cloning GitOps repo..."
          git clone "${REPO_URL}" "${WORKDIR}/gitops" || {
            echo "❌ Failed to clone GitOps repo"
            exit 1
          }
          
          cd "${WORKDIR}/gitops"
          
          # Update kustomization.yaml
          KUSTOMIZATION_FILE="apps/ml-recommendation-inference/overlays/${OVERLAY}/kustomization.yaml"
          
          if [ ! -f "${KUSTOMIZATION_FILE}" ]; then
            echo "❌ Error: File not found: ${KUSTOMIZATION_FILE}"
            exit 1
          fi
          
          echo "📝 Updating ${KUSTOMIZATION_FILE}..."
          
          # Use yq if available, otherwise use sed
          if command -v yq >/dev/null 2>&1; then
            # Update newName (ECR registry URL)
            yq eval -i ".images[] |= select(.name==\"inference\").newName = \"${REPO_URI}\"" "${KUSTOMIZATION_FILE}"
            # Update newTag
            yq eval -i ".images[] |= select(.name==\"inference\").newTag = \"${IMAGE_TAG}\"" "${KUSTOMIZATION_FILE}"
          else
            # Fallback: use sed to update lines
            # Update newName (match the line with newName)
            sed -i "s|newName:.*ml-fashion-recommender/inference|newName: ${REPO_URI}|g" "${KUSTOMIZATION_FILE}"
            # Update newTag (match the line with newTag after inference)
            sed -i "/name: inference/,/newTag:/ s|newTag:.*|newTag: ${IMAGE_TAG}|g" "${KUSTOMIZATION_FILE}"
          fi
          
          # Verify changes
          echo "📋 Updated kustomization.yaml:"
          grep -A 3 "name: inference" "${KUSTOMIZATION_FILE}" || true
          
          # Commit and push
          git add "${KUSTOMIZATION_FILE}"
          
          # Check if there are changes
          if git diff --staged --quiet; then
            echo "ℹ️  No changes to commit (tag already up-to-date)"
          else
            COMMIT_MSG="ci: update inference image to ${IMAGE_TAG} for ${ENV} (${GITHUB_SHA::7})"
            git commit -m "${COMMIT_MSG}"
            
            echo "🚀 Pushing changes to GitOps repo..."
            git push origin HEAD || {
              echo "❌ Failed to push to GitOps repo"
              exit 1
            }
            
            echo "✅ GitOps repo updated successfully"
            echo "📋 Commit: ${COMMIT_MSG}"
          fi
          
          # Cleanup
          rm -rf "${WORKDIR}"

      - name: Build Summary
        env:
          COMPONENT: ${{ env.COMPONENT }}
          REPO_URI: ${{ steps.docker.outputs.repo_uri }}
          PRIMARY_TAG: ${{ steps.docker.outputs.image_tag }}
          BUILD_TAG: ${{ steps.docker.outputs.build_tag }}
          LATEST_TAG: ${{ steps.docker.outputs.latest_tag }}
          GITOPS_TAG: ${{ steps.docker.outputs.gitops_tag }}
          ENV: ${{ steps.env.outputs.env }}
          OVERLAY: ${{ steps.env.outputs.gitops_overlay }}
          BUILD_TIMESTAMP: ${{ steps.env.outputs.build_timestamp }}
          RUN_ID: ${{ steps.env.outputs.run_id }}
          GITOPS_UPDATED: ${{ steps.gitops-check.outputs.enabled == 'true' && github.event_name != 'pull_request' }}
        run: |
          echo "### ✅ Build and Push Summary (BigTech ML Platform)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Property | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Component** | \`${COMPONENT}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Environment** | \`${ENV}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Primary Tag** (immutable) | \`${PRIMARY_TAG}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Build Tag** (traceable) | \`${BUILD_TAG}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Latest Tag** (mutable) | \`${LATEST_TAG}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **GitOps Tag** | \`${GITOPS_TAG}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Build Timestamp** | \`${BUILD_TIMESTAMP}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Run ID** | \`${RUN_ID}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **GitOps Updated** | $([ "${GITOPS_UPDATED}" == "true" ] && echo "✅ Yes" || echo "❌ No") |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Image URLs:**" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "${REPO_URI}:${PRIMARY_TAG}  # Immutable (primary)" >> $GITHUB_STEP_SUMMARY
          echo "${REPO_URI}:${BUILD_TAG}    # Traceable (build)" >> $GITHUB_STEP_SUMMARY
          echo "${REPO_URI}:${GITOPS_TAG}   # GitOps reference" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${GITOPS_UPDATED}" == "true" ]; then
            echo "✅ **ArgoCD will automatically sync** this image to \`${ENV}\` environment" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📋 **Tag Strategy (BigTech ML Platform Standard):**" >> $GITHUB_STEP_SUMMARY
            echo "- **Primary Tag**: Full SHA (immutable, always unique per commit) - **Used in GitOps**" >> $GITHUB_STEP_SUMMARY
            echo "- **Build Tag**: Short SHA + timestamp (traceable, for debugging)" >> $GITHUB_STEP_SUMMARY
            echo "- **Latest Tag**: Branch-latest (mutable, convenience reference only)" >> $GITHUB_STEP_SUMMARY
            echo "- **GitOps Tag**: \`${GITOPS_TAG}\` (immutable primary tag - ensures reproducibility)" >> $GITHUB_STEP_SUMMARY
          else
            echo "ℹ️  GitOps update skipped (PR or secrets not configured)" >> $GITHUB_STEP_SUMMARY
          fi
